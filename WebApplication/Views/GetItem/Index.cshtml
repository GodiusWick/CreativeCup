@model WebApplication.ViewModels.AllImports

<div>
    @{
        for (int i = 0; i < Model.allIports.Count; i++)
        {
            @Html.Partial("AllImports", Model.allIports[i]);
        }
    }
</div>
